/* export function maskCreditCardCode(code: string): string {
	if (code.length !== 16) return 'Invalid Credit/Debit card format!';
	return `${code.substring(0, 5)} ${code.substring(5, 10)} ${code.substring}`;
} */

export {};
